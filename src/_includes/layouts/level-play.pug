doctype html
html(lang=lang?lang:'nl')
    include _includes/layouts/head.pug
    include _includes/mixins/level-mixins.pug
    body
        main#swup
            //- play header (hidden for screenreaders because it would be disctracting)
            header(aria-hidden="true")
                a(href=`https://accessibility-training-final.netlify.app/simulationOverview` rel="prev" tabindex="-1").button.light.back Ga terug
                a(href="https://accessibility-training-final.netlify.app/simulationOverview" tabindex="-1")#logo
                    img(src='/assets/images/acc_logo.svg' alt='Logo van Stichting Accessibility')
                    h2 Accessibility

            .spacer(style="height: 4.5rem") 

            #game
                | !{content}

            
            //- DEBUG shortcut
            //- button#skip-level(onclick="showScore(100)" title="Sla level over")

            +level-score(0)